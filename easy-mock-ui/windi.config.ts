import { defineConfig } from 'windicss/helpers';

export default defineConfig({
  theme: {
    colors: {
      attraction: '#4300b0',
    },
    extend: {
      width: {
        fit: 'fit-content',
      },
    },
  },
});
